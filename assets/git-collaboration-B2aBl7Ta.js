var e="# Git 协作与交付流程\n\nOAT 使用 Git 作为唯一的代码协作边界；Admin 是控制面，Leader 是 review 和集成责任人，Worker 只交付可审核的短生命周期分支。\n\n## 流程\n\n1. Admin 将 WorkItem 分配给 Leader。\n2. Leader 仅将真正独立的实现任务派给 Worker。\n3. Worker 为每个任务创建 `oat/<team>/<taskId>/attempt-<n>` 分支，起点固定为任务创建时的 `main/master` SHA。\n4. Worker 完成实现和自测后调用 `submit-review`。该操作提交分支并把 commit、变更文件、测试结果及工件路径写入本地 review request；不会合并代码。\n5. Leader 调用 `list-review-requests` 检查证据，并以 `review-worker-branch` 批准合并到 `oat/<team>/<workItem>/integration`，或要求修改。\n6. Leader 在 integration 分支通过集成测试后调用 `submit-release-proposal`。\n7. Admin 用 `approve-release` 批准或拒绝。批准后 MergeController 使用全局串行锁和 Git `update-ref` 原子更新 `main/master`；若主分支已变化，发布失败而不会覆盖新提交。\n8. 远程发布是可选能力。配置 `workspace.git.push_enabled`、有效身份和 remote 后，由 Admin 显式调用 `push-release`；它只会对当前已合并 release 执行非强制推送。\n\n所有 Agent worktree 均禁用 push URL，Agent 子进程也不会继承 Git/SSH 凭据环境变量。唯一受支持的远程写入路径是 Orchestrator 主进程中经过角色校验的 Admin 工具。\n\n## 工件与临时文件\n\n运行工件在 `<runtime.persistence.state_dir>/git-collaboration/`：\n\n- `tasks/`：任务 branch、base SHA、测试证据和变更文件清单；\n- `reviews/`：review request 和 Leader 结论；\n- `releases/`：Admin 发布决策与 merge commit；\n- `worktrees/`：Worker、Leader 和 MergeController 的临时 worktree。\n\n任务 worktree 只包含项目源代码、测试与刻意纳入版本控制的文档。日志、Agent 元数据、草稿和会话文件均不得写入 Git worktree，避免被提交为业务产物。\n\n## API\n\n- `GET /reviews?leaderId=<id>`\n- `POST /reviews/<id>`：`{ leaderId, approve, note }`\n- `GET /releases`\n- `POST /releases/<id>/approval`：`{ adminId, approve, note }`\n- `GET /git/status` 与 `PUT /git/config`（刻意不提供通用 HTTP push 接口）\n\n所有向上汇报引用 `artifactPath`、branch、base/head SHA、changedFiles 和 tests，而不是复制完整工作日志。\n";export{e as default};